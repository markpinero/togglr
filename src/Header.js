import React from 'react';
import {Navbar, NavbarBrand} from './styled-components';

const Header = (props) => (
  <Navbar role="navigation">
    <NavbarBrand>Togglr</NavbarBrand>
  </Navbar>
  )

export default Header;